/**
 * ? isNan function
 * Terkadang kita buuh mengecek apakan sebuah nuber itu NaN atau bukan
 * methode ini menghasilkan nilai boolean true atau false
 * unutk melakuka pengecekan tersebut kita bisa menggunakan
 ** function isNaN(number)
 */

console.log(isNaN(NaN)); // Output : true
 